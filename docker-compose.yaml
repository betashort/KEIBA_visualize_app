version: "3.8"
services:
  react-app:
    build:
      dockerfile: ./docker/node/Dockerfile
    container_name: react-dev
    working_dir: /app

    volumes:
      - .:/app

    ports:
      - "5173:5173"

    tty: true

    stdin_open: true

    environment:
      - HOST=0.0.0.0
